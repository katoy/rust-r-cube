name: Build & Test

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build:
    name: Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry and git index
        uses: actions/cache@v4
        id: cargo-registry
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache sccache binary
        uses: actions/cache@v4
        id: sccache-bin
        with:
          path: ~/.cargo/bin
          key: ${{ runner.os }}-sccache-bin-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-sccache-bin-

      - name: Cache sccache state
        uses: actions/cache@v4
        id: sccache-cache
        with:
          path: |
            ~/.cache/sccache
          key: ${{ runner.os }}-sccache-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-sccache-

      - name: Cache target directory
        uses: actions/cache@v4
        id: cargo-target
        with:
          path: target
          key: ${{ runner.os }}-cargo-target-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-target-${{ matrix.rust }}-

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}

      - name: Install sccache (only on cache miss)
        if: steps.sccache-bin.outputs.cache-hit != 'true'
        shell: bash
        run: |
          cargo install sccache --locked

      - name: Show rustc and sccache
        shell: bash
        run: |
          rustc --version || true
          which sccache || true

      - name: Prefetch dependencies
        shell: bash
        run: |
          cd 2x2 && cargo fetch --locked || true
          cd ../2x2-web && cargo fetch --locked || true
          cd ../3x3-web && cargo fetch --locked || true

      - name: Build
        shell: bash
        run: |
          if [ -z "${CARGO_HOME:-}" ]; then
            CARGO_HOME="$HOME/.cargo"
          fi
          export PATH="$CARGO_HOME/bin:$PATH"
          if [ -x "$CARGO_HOME/bin/sccache" ]; then
            export RUSTC_WRAPPER="$CARGO_HOME/bin/sccache"
          fi
          cd 2x2 && cargo build --verbose
          cd ../2x2-web && cargo build --verbose
          cd ../3x3-web && cargo build --verbose

      - name: Test
        shell: bash
        run: |
          if [ -z "${CARGO_HOME:-}" ]; then
            CARGO_HOME="$HOME/.cargo"
          fi
          export PATH="$CARGO_HOME/bin:$PATH"
          if [ -x "$CARGO_HOME/bin/sccache" ]; then
            export RUSTC_WRAPPER="$CARGO_HOME/bin/sccache"
          fi
          cd 2x2 && cargo test --verbose
          cd ../2x2-web && cargo test --verbose
          cd ../3x3-web && cargo test --verbose
