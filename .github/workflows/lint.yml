name: Lint & Format

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  lint:
    name: Format & Clippy
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain and components
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Check formatting
        run: |
          cd 2x2 && cargo fmt --all -- --check
          cd ../2x2-web && cargo fmt --all -- --check
          cd ../3x3-web && cargo fmt --all -- --check

      - name: Run clippy
        run: |
          cd 2x2 && cargo clippy --all-targets --all-features -- -D warnings
          cd ../2x2-web && cargo clippy --all-targets --all-features -- -D warnings
          cd ../3x3-web && cargo clippy --all-targets --all-features -- -D warnings
